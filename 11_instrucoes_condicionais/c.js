let nome = 'joao';
let verificacao = nome == "joao" ? "sim" : "nao";
console.log("o utilizador está autorizado? " + verificacao);

// lembrado que esta forma de condição so pode ser usado se apenas uma condição for possivel 
