// length pode nao corresponder ao numero total de elementos do array
// length não conta o numero de elementos. ve apenas o ultimo indice e acrescenta mais um

let nomes = ['joão', 'ana', 'carlos'];
nomes[100] = 'pedro'
console.log(nomes)

console.log(nomes.length)