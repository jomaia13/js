// como fazer uma iteração(ciclo, laço) em um array

/**
 * ja vimos anteriormente os ciclos (laços) em javascript
 * vamos olhar para o array em especial, é uma coleção de dados podemos fazer uma interação sobre o array
 * 
 */
console.clear()

let nomes = ['vaneilza', 'pamela', 'cristina', 'luciana', 'bruna', 'diana'];

// com o ciclo for
for(let i = 0; i < nomes.length; i++){
    console.log(nomes[i])
}