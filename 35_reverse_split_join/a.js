// vamos ver mais alguns metodos para tratamento de arrays
// reverse split e join

console.clear()

// como o proprio nome diz, o reverse faz a inversao da ordem de um array
// nao contem qualquer argumento no metodo

let nomes = ['vaneilza', 'cristina', 'bruna', 'pamela', 'luciana', 'clesia']; 

console.table(nomes.reverse())

// este metodo altera o proprio array