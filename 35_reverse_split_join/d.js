// vamos ver mais alguns metodos para tratamento de arrays
// reverse split e join

console.clear()

// ainda dobre o split podemos separ uma string por letras
// para isso especificamos como valor do primeiro argumento uma string vazia

let texto = "separar as letras"
let colecao = texto.split('')
console.table(colecao)